apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: qcon-ingress
spec:
  rules:
  - http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          serviceName: qcon-service
          servicePort: 8000
status: {}