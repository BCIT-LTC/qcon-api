apiVersion: v1
kind: Service
metadata:
  name: qcon-service
  labels:
    app: qcon
    tier: frontend
#  namespace: web-apps
spec:
  type: NodePort
  selector:
    app: qcon
    tier: frontend
  ports:
  - port: 8000
    targetPort: 8000