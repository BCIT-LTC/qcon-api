namespace: qcon-api

resources:
  - ../../base
  - configmap.yaml

patchesStrategicMerge:
  - deployment.yaml
  - ingress.yaml

secretGenerator:
- name: star-dev-ltc-bcit-ca
  files:
    - "tls.crt"
    - "tls.key"
  type: "kubernetes.io/tls"