apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qcon-api
spec:
  rules:
  - host: staging.dev.ltc.bcit.ca
    http:
      paths:
      - path: /web-apps/qcon/qcon-api
        pathType: Prefix
        backend:
          service:
            name: qcon-api
            port:
              number: 8000