apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: qcon-api-ingress
spec:
  tls:
  - hosts:
      - qcon-api.dev.ltc.bcit.ca
    secretName: star-dev-ltc-bcit-ca
  rules:
  - host: staging.dev.ltc.bcit.ca
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: qcon-api
            port:
              number: 8000